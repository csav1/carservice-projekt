<Window x:Class="carservice_projekt.wartungseinträge"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fahrzeuginformationen" Height="600" Width="800" MinWidth="500" MinHeight="450" WindowStartupLocation="CenterScreen"
        Background="#FF1B262C">
    <Window.Resources>
        <SolidColorBrush x:Key="TextColor" Color="#FFE0E1DD"/>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            
            <Setter Property="FontSize" Value="16"/>
            
            <Setter Property="Padding" Value="5"/>
            
            <Setter Property="Background" Value="#FF274C77"/>
            
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            
            <Setter Property="BorderBrush" Value="#FF1F4287"/>
            
            <Setter Property="BorderThickness" Value="1.5"/>
            
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            
        </Style>
        
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            
            <Setter Property="VerticalAlignment" Value="Center"/>
            
            <Setter Property="Margin" Value="5"/>
            
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
        </Style>
        
        
        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Width" Value="120"/>
            
            <Setter Property="Height" Value="40"/>
            
            <Setter Property="Margin" Value="10"/>
            
            <Setter Property="Background" Value="#FF1F4287"/>
            
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            
            <Setter Property="FontWeight" Value="SemiBold"/>
            
            <Setter Property="Cursor" Value="Hand"/>
            
            <Setter Property="BorderThickness" Value="0"/>
            
            <Setter Property="HorizontalAlignment" Value="Center"/>
            
            <Setter Property="Template">
                
                
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    
                    <Setter Property="Background" Value="#FF274C77"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <StackPanel Margin="20">
        <TextBlock Text="Fahrzeug-Informationen" FontSize="28" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <Grid Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                
                <RowDefinition Height="Auto"/>
                
                <RowDefinition Height="Auto"/>
                
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Marke:" Grid.Row="0" Grid.Column="0"/>
            <TextBox x:Name="txtMarke" Grid.Row="0" Grid.Column="1"/>

            <TextBlock Text="Modell:" Grid.Row="1" Grid.Column="0"/>
            <TextBox x:Name="txtModell" Grid.Row="1" Grid.Column="1"/>

            <TextBlock Text="Baujahr:" Grid.Row="2" Grid.Column="0"/>
            <TextBox x:Name="txtBaujahr" Grid.Row="2" Grid.Column="1"/>

            <TextBlock Text="Kennzeichen:" Grid.Row="3" Grid.Column="0"/>
            <TextBox x:Name="txtKennzeichen" Grid.Row="3" Grid.Column="1"/>
        </Grid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            
            <Button Content="Speichern" Click="Speichern1"/>

            <Label x:Name="lblStatus"
       Content=""
       Foreground="LightGreen"
       FontSize="16"
       HorizontalAlignment="Center"
       Margin="0,10,0,0"/>

            <Button Content="Weiter" Click="BtnWeiter_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
